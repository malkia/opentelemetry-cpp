receivers:
  otlp/local:
    protocols:
      grpc:
        endpoint: 127.0.0.1:4317

exporters:
  file/metrics1:
    path: metrics1.json

service:
  pipelines:
    metrics:
      receivers: [otlp/local]
      exporters: [file/metrics1]
